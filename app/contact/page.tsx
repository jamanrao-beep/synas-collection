import ContactFormSection from '@/components/ContactFormSection';

export default function ContactPage() {
    return (
        <main style={{ backgroundColor: '#ba8a60', minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center', padding: '80px 0' }}>
            <ContactFormSection />
        </main>
    );
}
